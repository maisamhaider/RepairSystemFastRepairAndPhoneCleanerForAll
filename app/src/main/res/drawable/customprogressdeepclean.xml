<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="3dip" />
            <gradient
                android:startColor="@color/colorBorderCL"
                android:centerColor="@color/colorBorderCL"
                android:centerY="0.75"
                android:endColor="@color/colorBorderCL"
                android:angle="270"
                />
        </shape>
    </item>

    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="3dip" />
                <gradient
                    android:startColor="@color/standard_white"
                    android:endColor="@color/standard_white"
                    android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>